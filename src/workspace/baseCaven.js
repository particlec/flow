import { Modal } from 'antd';
import React from 'react';
import { Topology } from '@topology/core';

import { Data } from './data';

function BaseCanvas() {
  // let canvas = new Topology('topo-canvas', {});
  // canvas.open(Data);
  return <div>mixue</div>;

  // <canvas id={'topo-canvas'}>dddddddddddddddddd</canvas>;
}
export default BaseCanvas;
